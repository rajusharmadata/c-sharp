// cheack a year is leap year or not
// leap year is a year which is divisible by 4 but not by 100 or divisible by 400
// 2000 is a leap year
// 1900 is not a leap year
// 2004 is a leap year
// 2003 is not a leap year
using System;
namespace std{

    class prgram{
        static void Main(string[] args){
            // Console.WriteLine("Enter a number");
            //     int number = int.Parse(Console.ReadLine());// take a integer input
            //     string str = (number % 4 == 0 && number % 100 != 0 ) || number % 400 ==0? "Leap year" : "Not a leap year";
            //     Console.WriteLine(str);
                // solution obj = new solution();
                // obj.useLoop();
                

                StringBild obj1 = new StringBild();
                obj1.print();

        }
    }
}